# MVP 收尾 · 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 xiaobaihe_app 的 4 项 MVP 缺口（#1 评论分页加载、#2 个人主页帖子/收藏列表、#4 multipart 图片上传、#6 冷启动 userId 恢复），让 Mock 模式下主线功能端到端可走通。

**Architecture:** 保持现有五层分层（UI → Application → Repository → apiCall → SDK → 后端），本次改动集中在 UI、Application、Data、Core 四层，**不修改 SDK 代码生成文件**（`sdk/api/*` 保持原样）。图片上传 multipart 用 `dart:io HttpClient` 手写（不引入 `package:http`）。前端写到新后端契约上，Mock 同步对齐，真实后端接口另行由后端同学实现（见后端接口规格文档）。

**Tech Stack:** Flutter 3 / Dart 3.11.4、Riverpod 2.6、GoRouter 14.8、SharedPreferences、已有的 `dart:io HttpClient` 网络层。

**相关文档：**
- [设计文档](../specs/2026-04-11-mvp-收尾-design.md)
- [后端接口规格](../../backend/2026-04-11-后端待补接口.md)

---

## 前置条件

1. **工作目录**：所有 Flutter 命令在 `xiaobaihe_app/` 下执行
2. **git 未初始化**：本项目当前不是 git 仓库，plan 中的 commit 步骤为**可选**（标注为 "Commit (optional)"）。建议在每个 Phase 结束后手动快照，防止回滚困难
3. **Flutter 版本**：`flutter --version` 应显示 Dart SDK ≥ 3.11.4
4. **运行模式**：本 plan 的所有自动化验证跑在 Mock 模式（`flutter run -t lib/main_mock.dart`），真实后端联调等后端接口完成后另行进行

---

## 文件结构总览

```
xiaobaihe_app/
├── lib/
│   ├── core/
│   │   ├── api/
│   │   │   └── api_adapter.dart                      [修改] 新增 apiPostMultipart()
│   │   └── auth/
│   │       └── jwt_decoder.dart                      [新增] JWT payload 解码
│   ├── features/
│   │   ├── auth/application/auth_notifier.dart       [修改] _init 用 JWT 恢复 userId
│   │   ├── post/
│   │   │   ├── data/post_repository.dart             [修改] 新增 uploadImageMultipart
│   │   │   └── presentation/
│   │   │       ├── post_detail_page.dart             [修改] 评论滚动加载、footer、删无用 import
│   │   │       └── post_editor_page.dart             [修改] 并行 + 事务化上传
│   │   └── profile/
│   │       ├── application/user_posts_notifier.dart  [新增] StateNotifier + Key + State
│   │       ├── data/user_repository.dart             [修改] 新增 fetchUserPosts/fetchUserFavorites
│   │       └── presentation/
│   │           ├── profile_page.dart                 [修改] 接入 UserPostList、删 _PostListPlaceholder
│   │           └── widgets/user_post_list.dart       [新增] 分页帖子列表组件
│   ├── mock/
│   │   ├── mock_data.dart                            [修改] 加 favoritesVisible、测试帖子补 45 评论
│   │   ├── mock_http.dart                            [修改] MockHttpClientRequest.add() 补实现
│   │   └── mock_router.dart                          [修改] 假 JWT、新路由、multipart 媒体处理
│   └── sdk/
│       └── data/gateway.dart                         [修改] GetUserResp 增 favoritesVisible 字段
└── test/
    ├── core/auth/jwt_decoder_test.dart               [新增] JWT decoder 单测
    └── features/profile/application/
        └── user_posts_notifier_test.dart             [新增] Notifier 状态机单测
```

**统计**：3 个新文件，10 个修改文件，2 个新测试文件。

---

## Phase 0：Audit（已在 plan 撰写阶段完成）

撰写本 plan 前已读完 `mock_http.dart`、`mock_router.dart`、`mock_data.dart`、`api_adapter.dart`、`sdk/api/api.dart`、`pubspec.yaml`。审计结论已内化到各 Task 的代码细节中。执行者可跳过 Phase 0。

---

## Phase 1：#6 userId 恢复（JWT 解码）

依赖：无。此阶段是 Phase 4（用户帖子列表）的前置，必须先完成。

### Task 1.1：写 JWT decoder 的失败测试

**Files:**
- Create: `xiaobaihe_app/test/core/auth/jwt_decoder_test.dart`

- [ ] **Step 1：创建测试文件**

把下列完整内容写入 `xiaobaihe_app/test/core/auth/jwt_decoder_test.dart`：

```dart
import 'dart:convert';
import 'package:flutter_test/flutter_test.dart';
import 'package:xiaobaihe_app/core/auth/jwt_decoder.dart';

/// 构造一个用于测试的最小 JWT：header.payload.sig
/// payload 用入参的 Map 编码
String _buildJwt(Map<String, dynamic> payload) {
  final header = base64Url
      .encode(utf8.encode('{"alg":"none","typ":"JWT"}'))
      .replaceAll('=', '');
  final payloadStr = base64Url
      .encode(utf8.encode(jsonEncode(payload)))
      .replaceAll('=', '');
  return '$header.$payloadStr.fake-sig';
}

void main() {
  group('decodeJwtPayload', () {
    test('解析正常 token 返回 payload map', () {
      final token = _buildJwt({'userId': 42, 'name': 'alice'});
      final payload = decodeJwtPayload(token);
      expect(payload, isNotNull);
      expect(payload!['userId'], 42);
      expect(payload['name'], 'alice');
    });

    test('两段式 token 返回 null', () {
      expect(decodeJwtPayload('header.payload'), isNull);
    });

    test('四段式 token 返回 null', () {
      expect(decodeJwtPayload('a.b.c.d'), isNull);
    });

    test('payload 不是合法 JSON 返回 null', () {
      final bogus = base64Url
          .encode(utf8.encode('not-json'))
          .replaceAll('=', '');
      expect(decodeJwtPayload('header.$bogus.sig'), isNull);
    });

    test('payload 是 JSON 数组返回 null', () {
      final arr = base64Url
          .encode(utf8.encode('[1,2,3]'))
          .replaceAll('=', '');
      expect(decodeJwtPayload('header.$arr.sig'), isNull);
    });

    test('base64url 需要 padding 的 token 能正常解析', () {
      // 构造一个长度需要补 = 才能 decode 的 payload
      final token = _buildJwt({'userId': 1}); // 通常 length % 4 不等于 0
      expect(decodeJwtPayload(token), isNotNull);
    });

    test('空字符串返回 null', () {
      expect(decodeJwtPayload(''), isNull);
    });
  });

  group('extractUserIdFromToken', () {
    test('payload 含 userId 返回数值', () {
      final token = _buildJwt({'userId': 123});
      expect(extractUserIdFromToken(token), 123);
    });

    test('payload 无 userId 返回 null', () {
      final token = _buildJwt({'name': 'alice'});
      expect(extractUserIdFromToken(token), isNull);
    });

    test('token 解析失败返回 null', () {
      expect(extractUserIdFromToken('garbage'), isNull);
    });
  });
}
```

- [ ] **Step 2：跑测试，确认失败**

```
cd xiaobaihe_app
flutter test test/core/auth/jwt_decoder_test.dart
```

**Expected**：编译失败，错误类似 `Target of URI doesn't exist: 'package:xiaobaihe_app/core/auth/jwt_decoder.dart'`（因为被测文件还不存在）。

### Task 1.2：实现 JWT decoder 让测试通过

**Files:**
- Create: `xiaobaihe_app/lib/core/auth/jwt_decoder.dart`

- [ ] **Step 1：写实现**

创建 `xiaobaihe_app/lib/core/auth/jwt_decoder.dart`，内容：

```dart
import 'dart:convert';

/// 解析 JWT payload（不验签，只解内容）。
/// token 必须是标准三段式 "header.payload.signature"。
/// 解析失败返回 null。
Map<String, dynamic>? decodeJwtPayload(String token) {
  if (token.isEmpty) return null;
  try {
    final parts = token.split('.');
    if (parts.length != 3) return null;
    final normalized = base64Url.normalize(parts[1]);
    final decoded = utf8.decode(base64Url.decode(normalized));
    final json = jsonDecode(decoded);
    return json is Map<String, dynamic> ? json : null;
  } catch (_) {
    return null;
  }
}

/// 从 token 的 payload 中提取 userId。
/// 前提：后端保证 JWT payload 含有字段 `userId`（数值）。
/// 失败返回 null。
num? extractUserIdFromToken(String token) {
  final payload = decodeJwtPayload(token);
  final raw = payload?['userId'];
  if (raw is num) return raw;
  return null;
}
```

- [ ] **Step 2：跑测试，确认通过**

```
cd xiaobaihe_app
flutter test test/core/auth/jwt_decoder_test.dart
```

**Expected**：`All tests passed!`，10 个测试用例全绿。

- [ ] **Step 3：checkpoint（可选 commit）**

如果已初始化 git：
```
git add xiaobaihe_app/lib/core/auth/jwt_decoder.dart xiaobaihe_app/test/core/auth/jwt_decoder_test.dart
git commit -m "feat(core/auth): add JWT payload decoder with tests"
```

否则跳过。

### Task 1.3：把 decoder 接到 auth_notifier._init

**Files:**
- Modify: `xiaobaihe_app/lib/features/auth/application/auth_notifier.dart`

- [ ] **Step 1：修改 import**

在 `auth_notifier.dart` 文件顶部的 import 区（第 1-5 行附近），现有：
```dart
import 'package:flutter/foundation.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../../sdk/data/tokens.dart';
import '../../../sdk/vars/kv.dart';
```

在最后一行之后追加一行：
```dart
import '../../../core/auth/jwt_decoder.dart';
```

- [ ] **Step 2：重写 `_init` 方法**

找到 `auth_notifier.dart` 的 `_init()` 方法（约第 48-60 行），整体替换为：

```dart
Future<void> _init() async {
  final tokens = await getTokens();
  if (tokens != null && tokens.accessToken.isNotEmpty) {
    final userId = extractUserIdFromToken(tokens.accessToken);
    if (userId == null || userId <= 0) {
      // token 异常：清掉，按未登录处理
      await removeTokens();
      state = const AuthState(isLoading: false);
    } else {
      state = AuthState(
        isAuthenticated: true,
        userId: userId,
        token: tokens.accessToken,
        isLoading: false,
      );
    }
  } else {
    state = const AuthState(isLoading: false);
  }
  _changeNotifier.notify();
}
```

- [ ] **Step 3：`flutter analyze` 确认无新 warning**

```
cd xiaobaihe_app
flutter analyze lib/features/auth/application/auth_notifier.dart
```

**Expected**：0 error，不新增 warning（允许保留 info 级 SDK 生成的遗留问题）。

- [ ] **Step 4：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/features/auth/application/auth_notifier.dart
git commit -m "feat(auth): restore userId via JWT decoding on cold start"
```

### Task 1.4：Mock 登录/注册返回假 JWT

**Files:**
- Modify: `xiaobaihe_app/lib/mock/mock_router.dart`

- [ ] **Step 1：在文件顶部 import 区追加 import**

`mock_router.dart` 当前第 1-2 行：
```dart
import 'dart:convert';
import 'mock_data.dart';
```

当前 import 已经包含 `dart:convert`，无需新增 import。

- [ ] **Step 2：在 `_handleAuth` 函数上方新增假 JWT 工具**

找到 `mock_router.dart` 的 `// ─── Auth ───` 注释（约第 94 行），在这行下面、`Map<String, dynamic> _handleAuth` 函数之前插入：

```dart
/// 构造用于测试的假 JWT：header.{"userId":<id>}.fake-sig
String _buildFakeJwt(int userId) {
  final header = base64Url
      .encode(utf8.encode('{"alg":"none","typ":"JWT"}'))
      .replaceAll('=', '');
  final payload = base64Url
      .encode(utf8.encode('{"userId":$userId}'))
      .replaceAll('=', '');
  return '$header.$payload.fake-sig';
}
```

- [ ] **Step 3：把 login/register 的 token 改为假 JWT**

找到 `_handleAuth` 函数内的 login 和 register case（约第 103-106 行），当前：

```dart
case 'login':
  return {'userId': 1, 'token': 'mock_token_${DateTime.now().millisecondsSinceEpoch}'};
case 'register':
  return {'userId': 1, 'token': 'mock_token_${DateTime.now().millisecondsSinceEpoch}'};
```

整体替换为：

```dart
case 'login':
  return {'userId': 1, 'token': _buildFakeJwt(1)};
case 'register':
  return {'userId': 1, 'token': _buildFakeJwt(1)};
```

- [ ] **Step 4：`flutter analyze` 确认无 warning**

```
cd xiaobaihe_app
flutter analyze lib/mock/mock_router.dart
```

**Expected**：0 error，0 新增 warning。

- [ ] **Step 5：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/mock/mock_router.dart
git commit -m "feat(mock): return fake JWT tokens on login/register"
```

### Task 1.5：手动验证 Phase 1

- [ ] **Step 1：启动 Mock 应用**

```
cd xiaobaihe_app
flutter run -t lib/main_mock.dart -d windows
```

（如无 Windows 桌面支持改用可用 device：`flutter devices` 查看）

- [ ] **Step 2：验证"冷启动已登录 → 不跳登录"场景**

1. 应用启动后先进入登录页，用任意用户名密码登录（Mock 无校验）
2. 登录成功进入首页
3. 停止应用（关窗口或按 `q`）
4. 重新启动：`flutter run -t lib/main_mock.dart -d windows`
5. 启动后直接点击底部 tab "个人中心"
6. **Expected**：直接看到个人中心页面（非"请先登录"占位），显示 userId=1 的用户信息

- [ ] **Step 3：验证"冷启动未登录 → 跳登录"场景**

1. 在个人中心点右上角登出按钮
2. 停止应用、重启
3. 点底部 tab "个人中心"
4. **Expected**：显示"请先登录"页，点击"去登录"跳转到登录页

如两步都符合预期，Phase 1 完成。

---

## Phase 2：#1 评论分页加载

依赖：无。此阶段可与 Phase 1 并行或互换顺序。

### Task 2.1：给 post_detail_page 加 ScrollController 和触底监听

**Files:**
- Modify: `xiaobaihe_app/lib/features/post/presentation/post_detail_page.dart`

- [ ] **Step 1：删除未用 import**

找到 `post_detail_page.dart` 第 5 行：
```dart
import '../../../core/api/api_adapter.dart';
```

**删除整行**。

- [ ] **Step 2：在 state 类中新增 ScrollController 字段**

找到 `_PostDetailPageState` 类（约第 33 行），当前字段声明块：

```dart
class _PostDetailPageState extends ConsumerState<PostDetailPage> {
  List<CommentItem> _comments = [];
  bool _isLoadingComments = false;
  int _commentPage = 1;
  bool _hasMoreComments = true;
  int _commentSortBy = 1;
```

在 `int _commentSortBy = 1;` 之后、`// 乐观更新状态` 注释之前插入一行：

```dart
  final ScrollController _scrollCtrl = ScrollController();
```

- [ ] **Step 3：重写 `initState` 并新增 `dispose`**

找到 `initState` 方法（约第 51 行），当前：

```dart
@override
void initState() {
  super.initState();
  _loadComments();
}
```

整体替换为：

```dart
@override
void initState() {
  super.initState();
  _scrollCtrl.addListener(_onScroll);
  _loadComments();
}

@override
void dispose() {
  _scrollCtrl.removeListener(_onScroll);
  _scrollCtrl.dispose();
  super.dispose();
}

void _onScroll() {
  if (!_scrollCtrl.hasClients) return;
  final threshold = _scrollCtrl.position.maxScrollExtent - 300;
  if (_scrollCtrl.position.pixels >= threshold &&
      !_isLoadingComments &&
      _hasMoreComments) {
    _commentPage++;
    _loadComments();
  }
}
```

- [ ] **Step 4：把 controller 挂到 CustomScrollView**

找到 `build` 方法内的 `CustomScrollView`（约第 189 行）：

```dart
Expanded(
  child: CustomScrollView(
    slivers: [
```

改为：

```dart
Expanded(
  child: CustomScrollView(
    controller: _scrollCtrl,
    slivers: [
```

- [ ] **Step 5：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/features/post/presentation/post_detail_page.dart
```

**Expected**：原先的 `unused_import` 和 `unused_field _hasMoreComments` 警告消失，无新增 warning/error。

- [ ] **Step 6：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/features/post/presentation/post_detail_page.dart
git commit -m "feat(post): auto-load more comments on scroll to bottom"
```

### Task 2.2：修复评论排序切换时的状态重置

**Files:**
- Modify: `xiaobaihe_app/lib/features/post/presentation/post_detail_page.dart`

- [ ] **Step 1：重写排序切换回调**

找到 `SegmentedButton<int>` 的 `onSelectionChanged`（约第 317-322 行），当前：

```dart
onSelectionChanged: (v) {
  setState(() {
    _commentSortBy = v.first;
    _commentPage = 1;
  });
  _loadComments();
},
```

整体替换为：

```dart
onSelectionChanged: (v) {
  setState(() {
    _commentSortBy = v.first;
    _commentPage = 1;
    _hasMoreComments = true;
    _comments = [];
  });
  _loadComments();
},
```

**关键点**：`_hasMoreComments` 必须重置为 `true`，否则上一次"到底"后切排序会无法触发加载；`_comments` 必须清空，否则新排序的评论会追加到旧列表尾部。

- [ ] **Step 2：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/features/post/presentation/post_detail_page.dart
```

**Expected**：无 error / 无新 warning。

### Task 2.3：追加"没有更多了"footer

**Files:**
- Modify: `xiaobaihe_app/lib/features/post/presentation/post_detail_page.dart`

- [ ] **Step 1：修改 SliverList childBuilder**

找到评论 `SliverList`（约第 347-377 行），当前结构：

```dart
SliverList(
  delegate: SliverChildBuilderDelegate(
    (context, index) {
      if (index >= topLevel.length) {
        return _isLoadingComments
            ? const Padding(
                padding: EdgeInsets.all(16),
                child: Center(
                    child:
                        CircularProgressIndicator()),
              )
            : null;
      }
      final comment = topLevel[index];
      return CommentItemWidget(
        comment: comment,
        replies:
            repliesMap[comment.id] ?? [],
        onReply: () {
          setState(() {
            _replyToUser = comment.userName;
            _replyParentId = comment.id;
            _replyUserId = comment.userId;
          });
        },
      );
    },
    childCount:
        topLevel.length + (_isLoadingComments ? 1 : 0),
  ),
),
```

整体替换为：

```dart
SliverList(
  delegate: SliverChildBuilderDelegate(
    (context, index) {
      if (index >= topLevel.length) {
        // tail 位置：优先显示加载中，否则显示"没有更多了"
        if (_isLoadingComments) {
          return const Padding(
            padding: EdgeInsets.all(16),
            child: Center(child: CircularProgressIndicator()),
          );
        }
        if (!_hasMoreComments && topLevel.isNotEmpty) {
          return Padding(
            padding: const EdgeInsets.symmetric(vertical: 20),
            child: Center(
              child: Text(
                '— 没有更多了 —',
                style: TextStyle(
                  color: Theme.of(context).colorScheme.outline,
                  fontSize: 12,
                ),
              ),
            ),
          );
        }
        return null;
      }
      final comment = topLevel[index];
      return CommentItemWidget(
        comment: comment,
        replies: repliesMap[comment.id] ?? [],
        onReply: () {
          setState(() {
            _replyToUser = comment.userName;
            _replyParentId = comment.id;
            _replyUserId = comment.userId;
          });
        },
      );
    },
    childCount: topLevel.length +
        ((_isLoadingComments || (!_hasMoreComments && topLevel.isNotEmpty))
            ? 1
            : 0),
  ),
),
```

- [ ] **Step 2：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/features/post/presentation/post_detail_page.dart
```

**Expected**：无 error、无新 warning。

### Task 2.4：给测试帖子补 45 条评论（Mock 数据）

**Files:**
- Modify: `xiaobaihe_app/lib/mock/mock_data.dart`

- [ ] **Step 1：给 postId=1 的评论列表追加 45 条**

找到 `mock_data.dart` 中的 `seedComments` map，找到 key `1:` 的条目（约第 173-207 行）。当前该 key 下有 3 条评论（id 1/2/3）。

在 id=3 的评论对象（以 `},` 结尾，约第 206 行）之后、`key 2:` 之前，插入以下**连续 45 条**评论（id 14 至 58，确保不与其它 key 的 id 冲突——现有最大 id 为 13）：

```dart
    // ─── 下列 45 条用于测试评论分页加载（Phase 2）───
    for (int i = 14; i <= 58; i++)
      {
        'id': i,
        'userId': (i % 3) + 1,
        'userName': ['小白鸽', '萌萌哒小兔', '科技宅小明'][i % 3],
        'userAvatar': '',
        'parentId': 0,
        'replyUserId': 0,
        'content': '压测评论 #$i —— 滚动到底后会自动加载下一页',
        'likeCount': i % 10,
        'createdAt': _now - 1000 - i * 10,
      },
```

**注意**：Dart 的 list literal 支持 `for` 语句（collection-for），上面是合法语法。45 条 + 原 3 条 = 48 条，pageSize=20 可分 3 页验证滚动加载。

- [ ] **Step 2：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/mock/mock_data.dart
```

**Expected**：无 error、无 warning。

- [ ] **Step 3：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/features/post/presentation/post_detail_page.dart xiaobaihe_app/lib/mock/mock_data.dart
git commit -m "feat(post): show 'no more comments' footer; seed test data"
```

### Task 2.5：手动验证 Phase 2

- [ ] **Step 1：热重启 Mock 应用**

如果 Phase 1 结束后应用还开着：按 `R`（大写 R）做热重启；否则：
```
cd xiaobaihe_app
flutter run -t lib/main_mock.dart
```

- [ ] **Step 2：验证"滑到底部自动加载下一页"**

1. 首页点击标题为"探店｜藏在巷子里的宝藏面馆"的帖子（id=1）
2. 滑到评论区
3. 快速滑到评论底部
4. **Expected**：自动触发加载更多，显示 loading 指示，然后追加 20 条新评论
5. 继续滑到新底部
6. **Expected**：再次触发加载，总计显示 48 条评论
7. 继续滑
8. **Expected**：显示"— 没有更多了 —"footer，不再触发请求

- [ ] **Step 3：验证"切换排序重置分页"**

1. 在评论区顶部把排序从"最新"切到"最热"
2. **Expected**：评论列表清空重新从第 1 页加载（显示前 20 条，可继续滑底加载）
3. 切回"最新"
4. **Expected**：同样从第 1 页开始、`_hasMoreComments=true`

如两步都符合预期，Phase 2 完成。

---

## Phase 3：#4 图片上传 multipart 事务化

依赖：无。Phase 1/2/3 可并行或任意顺序。

### Task 3.1：在 api_adapter.dart 新增 apiPostMultipart

**Files:**
- Modify: `xiaobaihe_app/lib/core/api/api_adapter.dart`

- [ ] **Step 1：修改 import 区**

当前 `api_adapter.dart` 头部：
```dart
import 'dart:async';
import 'api_exceptions.dart';
```

替换为：

```dart
import 'dart:async';
import 'dart:convert';
import 'dart:io';
import 'dart:math';
import '../../sdk/vars/kv.dart';
import '../../sdk/vars/vars.dart';
import 'api_exceptions.dart';
```

- [ ] **Step 2：在文件末尾追加 multipart 上传函数**

在 `apiCallWithTimeout` 函数（约第 50 行）之后、文件末尾追加：

```dart

/// Multipart POST 上传，用于文件上传场景。
///
/// 走与 `sdk/api/api.dart::apiPost` 相同的 baseUrl（`serverHost`）+
/// Authorization 头处理逻辑，但用 `HttpClient` 手工构造 multipart body
/// 以避免引入 `package:http`。
///
/// 响应格式仍然是后端统一的 `{code, desc, data}`，返回的是 `data` 里由
/// `decodeData` 解出的值。
Future<T> apiPostMultipart<T>({
  required String path,
  required String fieldName,
  required String filename,
  required List<int> bytes,
  required T Function(Map<String, dynamic>) decodeData,
  Duration timeout = const Duration(seconds: 60),
}) async {
  final boundary = '----dart-xiaobaihe-${_randomBoundary()}';
  final body = _buildMultipartBody(
    boundary: boundary,
    fieldName: fieldName,
    filename: filename,
    bytes: bytes,
  );

  final tokens = await getTokens();
  final client = HttpClient();
  client.connectionTimeout = timeout;

  try {
    final req = await client.postUrl(Uri.parse(serverHost + path));
    req.headers.set('Content-Type', 'multipart/form-data; boundary=$boundary');
    req.headers.set('Content-Length', body.length);
    if (tokens != null) {
      req.headers.set('Authorization', tokens.accessToken);
    }
    req.add(body);

    final rp = await req.close().timeout(timeout);
    final respBody = await rp.transform(utf8.decoder).join();

    if (rp.statusCode == 404) {
      throw const ApiException('404 not found');
    }

    final Map<String, dynamic> base = jsonDecode(respBody);
    if (base['code'] != 0) {
      throw ApiException(base['desc']?.toString() ?? 'upload failed');
    }
    return decodeData(base['data'] as Map<String, dynamic>);
  } on ApiException {
    rethrow;
  } catch (e) {
    throw ApiException(e.toString());
  } finally {
    client.close(force: true);
  }
}

String _randomBoundary() {
  final rnd = Random();
  return List.generate(16, (_) => rnd.nextInt(36).toRadixString(36)).join();
}

/// 手工构造 multipart/form-data body。单字段版本（只支持一个 file）。
List<int> _buildMultipartBody({
  required String boundary,
  required String fieldName,
  required String filename,
  required List<int> bytes,
}) {
  final crlf = '\r\n';
  final head = '--$boundary$crlf'
      'Content-Disposition: form-data; name="$fieldName"; filename="$filename"$crlf'
      'Content-Type: application/octet-stream$crlf'
      '$crlf';
  final tail = '$crlf--$boundary--$crlf';
  return [
    ...utf8.encode(head),
    ...bytes,
    ...utf8.encode(tail),
  ];
}
```

- [ ] **Step 3：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/core/api/api_adapter.dart
```

**Expected**：0 error。可能有 info 级 `unintended_html_in_doc_comment` 沿用现状，不新增 warning。

- [ ] **Step 4：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/core/api/api_adapter.dart
git commit -m "feat(core/api): add apiPostMultipart for file upload"
```

### Task 3.2：在 PostRepository 新增 uploadImageMultipart

**Files:**
- Modify: `xiaobaihe_app/lib/features/post/data/post_repository.dart`

- [ ] **Step 1：在 `PostRepository` 类内追加方法**

找到 `post_repository.dart` 的 `PostRepository` 类末尾（约第 51 行，`}` 之前）。在 `deleteExistingPost` 方法之后、类的闭合 `}` 之前，追加：

```dart

  /// 以 multipart 协议上传单张图片，返回图片 URL。
  ///
  /// 绕过 SDK `uploadImage`（JSON 字节数组协议），走 `apiPostMultipart`。
  Future<String> uploadImageMultipart({
    required List<int> bytes,
    required String filename,
  }) {
    return apiPostMultipart<String>(
      path: '/api/v1/media/image',
      fieldName: 'file',
      filename: filename,
      bytes: bytes,
      decodeData: (data) => data['url'] as String,
    );
  }
```

- [ ] **Step 2：修改 import 区，引入 apiPostMultipart**

`post_repository.dart` 当前第 1 行：
```dart
import '../../../core/api/api_adapter.dart';
```

**保持不变**（`apiPostMultipart` 定义在同文件，这行 import 已经覆盖）。

- [ ] **Step 3：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/features/post/data/post_repository.dart
```

**Expected**：0 error、0 新 warning。

- [ ] **Step 4：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/features/post/data/post_repository.dart
git commit -m "feat(post/data): add uploadImageMultipart method"
```

### Task 3.3：重写 post_editor_page 上传逻辑为并行 + 事务化

**Files:**
- Modify: `xiaobaihe_app/lib/features/post/presentation/post_editor_page.dart`

- [ ] **Step 1：修改 import，删除 SDK 上传依赖**

找到 `post_editor_page.dart` 第 1-9 行的 import 区，当前：

```dart
import 'dart:io';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import '../../../core/api/api_adapter.dart';
import '../../../sdk/api/gateway.dart' as gw;
import '../../../sdk/data/gateway.dart';
import '../data/post_repository.dart';
import 'widgets/image_picker_grid.dart';
```

替换为：

```dart
import 'dart:io';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import 'package:path/path.dart' as p;
import '../../../sdk/data/gateway.dart';
import '../data/post_repository.dart';
import 'widgets/image_picker_grid.dart';
```

**说明**：
- 删除 `core/api/api_adapter.dart`（上传走 Repository）
- 删除 `sdk/api/gateway.dart as gw`（不再直接调 `gw.uploadImage`）
- **检查：`package:path` 是否已在 pubspec 中**。打开 `xiaobaihe_app/pubspec.yaml`，如果 `dependencies` 没有 `path:`，在 `intl: ^0.19.0` 之后追加一行 `path: ^1.9.0`，然后运行 `flutter pub get`。
  - 如果 `path` 不好加（依赖冲突等），改用纯字符串处理代替：`filename: f.path.split(RegExp(r'[\\/]')).last`，并**移除** `import 'package:path/path.dart' as p;`。

- [ ] **Step 2：定义事务化异常类**

在 `post_editor_page.dart` 文件末尾（最后一个 `}` 之后）追加：

```dart

/// 图片批量上传的事务化异常：任一张失败即抛出，保留失败索引和原因。
class _UploadTransactionException implements Exception {
  final int failedIndex;
  final String reason;
  const _UploadTransactionException(this.failedIndex, this.reason);

  @override
  String toString() => '第 ${failedIndex + 1} 张图片上传失败：$reason';
}
```

- [ ] **Step 3：重写 `_uploadLocalImages` 方法**

找到 `post_editor_page.dart` 的 `_uploadLocalImages` 方法（约第 78-93 行），当前：

```dart
Future<List<String>> _uploadLocalImages() async {
  final urls = <String>[];
  for (final file in _localImages) {
    final bytes = await file.readAsBytes();
    final resp = await apiCall<UploadImageResp>(
      (ok, fail, eventually) => gw.uploadImage(
        UploadImageReq(file: bytes.toList()),
        ok: ok,
        fail: fail,
        eventually: eventually,
      ),
    );
    urls.add(resp.url);
  }
  return urls;
}
```

整体替换为：

```dart
Future<List<String>> _uploadLocalImages() async {
  if (_localImages.isEmpty) return const [];

  final repo = ref.read(_postRepoProvider);

  // 并行触发所有上传，每个任务返回 (index, url_or_error)
  final futures = <Future<(int, String?, String?)>>[];
  for (var i = 0; i < _localImages.length; i++) {
    final idx = i;
    final file = _localImages[i];
    futures.add(() async {
      try {
        final bytes = await file.readAsBytes();
        final name = p.basename(file.path); // 若不用 package:path 参见 Step 1 的替代方案
        final url = await repo.uploadImageMultipart(
          bytes: bytes,
          filename: name,
        );
        return (idx, url, null);
      } catch (e) {
        return (idx, null, e.toString());
      }
    }());
  }

  final results = await Future.wait(futures);

  // 事务化：找第一个失败的（按索引升序），有失败则整单丢弃
  results.sort((a, b) => a.$1.compareTo(b.$1));
  for (final r in results) {
    if (r.$2 == null) {
      throw _UploadTransactionException(r.$1, r.$3 ?? 'unknown');
    }
  }

  return [for (final r in results) r.$2!];
}
```

- [ ] **Step 4：修改 `_publish` 方法捕获事务异常并弹 Dialog**

找到 `_publish` 方法（约第 95-140 行），当前的 `try/catch` 块：

```dart
try {
  final uploadedUrls = await _uploadLocalImages();
  final allImages = [..._networkImages, ...uploadedUrls];

  if (_isEditMode) {
    await ref.read(_postRepoProvider).updateExistingPost(
          widget.postId!,
          UpdatePostReq(
            postId: widget.postId!,
            title: _titleCtrl.text.trim(),
            content: _contentCtrl.text.trim(),
            images: allImages,
            tags: _tags,
          ),
        );
    if (mounted) context.pop();
  } else {
    final resp = await ref.read(_postRepoProvider).createNewPost(
          CreatePostReq(
            title: _titleCtrl.text.trim(),
            content: _contentCtrl.text.trim(),
            images: allImages,
            tags: _tags,
            status: status,
          ),
        );
    if (mounted) {
      context.go('/post/${resp.postId.toInt()}');
    }
  }
} catch (e) {
  if (mounted) {
    ScaffoldMessenger.of(context)
        .showSnackBar(SnackBar(content: Text('发布失败: $e')));
  }
} finally {
  if (mounted) setState(() => _isLoading = false);
}
```

整体替换为：

```dart
try {
  final uploadedUrls = await _uploadLocalImages();
  final allImages = [..._networkImages, ...uploadedUrls];

  if (_isEditMode) {
    await ref.read(_postRepoProvider).updateExistingPost(
          widget.postId!,
          UpdatePostReq(
            postId: widget.postId!,
            title: _titleCtrl.text.trim(),
            content: _contentCtrl.text.trim(),
            images: allImages,
            tags: _tags,
          ),
        );
    if (mounted) context.pop();
  } else {
    final resp = await ref.read(_postRepoProvider).createNewPost(
          CreatePostReq(
            title: _titleCtrl.text.trim(),
            content: _contentCtrl.text.trim(),
            images: allImages,
            tags: _tags,
            status: status,
          ),
        );
    if (mounted) {
      context.go('/post/${resp.postId.toInt()}');
    }
  }
} on _UploadTransactionException catch (e) {
  // 事务化上传失败：弹 Dialog 明确提示、草稿保留（不清空 _localImages 等）
  if (mounted) {
    await showDialog<void>(
      context: context,
      builder: (ctx) => AlertDialog(
        title: const Text('图片上传失败'),
        content: Text(
          '${e.toString()}\n\n帖子未发布，图片已保留，可修改后重试。',
        ),
        actions: [
          TextButton(
            onPressed: () => Navigator.of(ctx).pop(),
            child: const Text('知道了'),
          ),
        ],
      ),
    );
  }
} catch (e) {
  if (mounted) {
    ScaffoldMessenger.of(context)
        .showSnackBar(SnackBar(content: Text('发布失败: $e')));
  }
} finally {
  if (mounted) setState(() => _isLoading = false);
}
```

- [ ] **Step 5：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/features/post/presentation/post_editor_page.dart
```

**Expected**：0 error。如果 `package:path` 未加 dep 且用了 `p.basename`，会报错，按 Step 1 的替代方案改字符串处理即可。

- [ ] **Step 6：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/features/post/presentation/post_editor_page.dart xiaobaihe_app/pubspec.yaml xiaobaihe_app/pubspec.lock
git commit -m "feat(post/editor): parallel transactional multipart image upload"
```

### Task 3.4：Mock 适配 multipart 上传

**Files:**
- Modify: `xiaobaihe_app/lib/mock/mock_http.dart`
- Modify: `xiaobaihe_app/lib/mock/mock_router.dart`

- [ ] **Step 1：补 `MockHttpClientRequest.add()` 方法**

`mock_http.dart` 当前 `MockHttpClientRequest` 只实现了 `write()`（第 46-48 行）：
```dart
@override
void write(Object? obj) {
  if (obj != null) _body = obj.toString();
}
```

在 `write()` 之后、`close()` 之前追加：

```dart
@override
void add(List<int> data) {
  // multipart 上传走 add(bytes)，Mock 不需要解析具体内容。
  // 仅记录一个占位字符串，让 mock_router 走 media 分支。
  if (data.isNotEmpty && _body.isEmpty) {
    _body = '<multipart-binary-${data.length}-bytes>';
  }
}
```

- [ ] **Step 2：改 `_handleMedia` 返回随机 URL**

`mock_router.dart` 的 `_handleMedia` 当前（约第 392-398 行）：

```dart
Map<String, dynamic> _handleMedia() {
  return {
    'mediaId': DateTime.now().millisecondsSinceEpoch,
    'url': 'https://picsum.photos/400/300',
    'thumbnailUrl': 'https://picsum.photos/200/150',
  };
}
```

替换为：

```dart
Map<String, dynamic> _handleMedia() {
  // 每次返回带随机后缀的 picsum URL，便于肉眼区分多张上传结果
  final seed = DateTime.now().microsecondsSinceEpoch % 10000;
  return {
    'mediaId': DateTime.now().millisecondsSinceEpoch,
    'url': 'https://picsum.photos/seed/$seed/400/300',
    'thumbnailUrl': 'https://picsum.photos/seed/$seed/200/150',
  };
}
```

- [ ] **Step 3：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/mock/mock_http.dart lib/mock/mock_router.dart
```

**Expected**：0 error、0 新 warning。

- [ ] **Step 4：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/mock/mock_http.dart xiaobaihe_app/lib/mock/mock_router.dart
git commit -m "feat(mock): support multipart upload; varied image URLs"
```

### Task 3.5：手动验证 Phase 3

- [ ] **Step 1：热重启 Mock 应用**

```
# 在运行中的 flutter run 按 R（大写）做 hot restart
# 或重启：
cd xiaobaihe_app
flutter run -t lib/main_mock.dart
```

- [ ] **Step 2：验证"3 张图全成功发布"**

1. 登录
2. 进入首页，点右上角 + 或底部发布入口进入 `post_editor_page`
3. 填标题、正文
4. 点图片选择器，从相册选 2-3 张图
5. 点"发布"
6. **Expected**：稍等后跳转到新帖子详情页，图片正常显示

- [ ] **Step 3：验证事务化失败（临时篡改 Mock 制造失败）**

这一步需要临时修改 Mock 让上传失败：

1. 打开 `xiaobaihe_app/lib/mock/mock_router.dart` 的 `_handleMedia` 方法
2. 临时在第一行加：`throw Exception('simulated upload failure');`
3. 在 Flutter 控制台按 `R`（hot restart）
4. 回 post_editor 页，选 3 张图，点"发布"
5. **Expected**：
   - 不跳转到帖子详情
   - 弹出 AlertDialog：「图片上传失败」「第 1 张图片上传失败：ApiException: simulated upload failure」「帖子未发布，图片已保留，可修改后重试。」
   - 点"知道了"关闭 Dialog
   - **草稿保留**：标题、正文、选中的 3 张图缩略图还在，按钮可重新点击
6. **撤销临时修改**：把刚才加的 `throw` 行删掉，按 `R` 热重启
7. 再点"发布"
8. **Expected**：恢复全成功路径，跳转成功

如两步都符合预期，Phase 3 完成。

---

## Phase 4：#2 用户帖子/收藏列表

依赖：**Phase 1 必须完成**（需要 userId 恢复才能看到"我的收藏"场景）。

### Task 4.1：扩展 GetUserResp 增加 favoritesVisible 字段

**Files:**
- Modify: `xiaobaihe_app/lib/sdk/data/gateway.dart`

> **注意**：本 Task 修改的是代码生成文件。按 CLAUDE.md 的约定，此处作为"前端预先写到新契约上"的一次性改动，字段名（`favoritesVisible`）已与后端规格文档对齐（[后端接口规格](../../backend/2026-04-11-后端待补接口.md)）。后端重新生成 SDK 时将覆盖本改动，但字段应该会重新出现（因为后端已按规格实现）。

- [ ] **Step 1：找到 `GetUserResp` 类**

打开 `xiaobaihe_app/lib/sdk/data/gateway.dart`，搜索 `class GetUserResp`。会看到形如：

```dart
class GetUserResp {
  final num id;
  final String username;
  final String nickname;
  final String avatarUrl;
  final String bio;
  final num level;
  final num followerCount;
  final num followingCount;
  final num postCount;
  GetUserResp({...});
  factory GetUserResp.fromJson(Map<String, dynamic> m) {...}
  Map<String, dynamic> toJson() {...}
}
```

- [ ] **Step 2：追加 `favoritesVisible` 字段**

按如下模式 3 处追加（具体根据现有字段顺序定位）：

1. 在 `final num postCount;` 之后追加：
```dart
  // 收藏列表可见性：true=公开，false=私密。后端契约见 docs/backend/2026-04-11-后端待补接口.md
  final bool favoritesVisible;
```

2. 在构造函数参数列表最后追加 `required this.favoritesVisible,`（保持与其它 required 字段一致的风格）。

3. 在 `fromJson` 工厂里最后追加 `favoritesVisible: m['favoritesVisible'] ?? true,`（默认 true 作为兜底，防止后端未返回时崩溃）。

4. 在 `toJson` 方法的返回 map 最后追加 `'favoritesVisible': favoritesVisible,`。

- [ ] **Step 3：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/sdk/data/gateway.dart
```

**Expected**：0 error。可能有 info 级的现有告警（`unnecessary_brace_in_string_interps`），忽略。

- [ ] **Step 4：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/sdk/data/gateway.dart
git commit -m "feat(sdk): add favoritesVisible to GetUserResp (synced with backend spec)"
```

### Task 4.2：在 mock_data 的 seedUsers 里加 favoritesVisible

**Files:**
- Modify: `xiaobaihe_app/lib/mock/mock_data.dart`

- [ ] **Step 1：给三个 seed user 都加字段**

找到 `seedUsers` map（约第 5-39 行），给 key 1、2、3 的每个用户对象追加一行：

- 用户 1（`'postCount': 12,` 之后）加：`'favoritesVisible': true,`
- 用户 2（`'postCount': 89,` 之后）加：`'favoritesVisible': true,`
- 用户 3（`'postCount': 34,` 之后）加：`'favoritesVisible': false,`

**理由**：用户 3 设为私密用于验证"他人私密收藏只显示帖子 tab"场景。

- [ ] **Step 2：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/mock/mock_data.dart
```

**Expected**：0 error、0 warning。

### Task 4.3：写 UserPostsNotifier 的失败测试

**Files:**
- Create: `xiaobaihe_app/test/features/profile/application/user_posts_notifier_test.dart`

- [ ] **Step 1：写测试文件**

创建 `xiaobaihe_app/test/features/profile/application/user_posts_notifier_test.dart`，内容：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:xiaobaihe_app/features/profile/application/user_posts_notifier.dart';
import 'package:xiaobaihe_app/sdk/data/gateway.dart';

/// Fake Repository 实现：可配置返回的数据和是否抛异常
class _FakeUserPostsRepo implements UserPostsRepository {
  List<GetPostListResp> pages = [];
  int calls = 0;
  bool shouldFail = false;
  String failReason = 'boom';

  void addPage(List<PostItem> items, {int total = 100}) {
    pages.add(GetPostListResp(
      list: items,
      total: total,
      page: pages.length + 1,
      pageSize: items.length,
    ));
  }

  @override
  Future<GetPostListResp> fetchUserPosts({
    required num userId,
    required int page,
    required int pageSize,
    int sortBy = 1,
  }) async {
    calls++;
    if (shouldFail) throw Exception(failReason);
    final idx = page - 1;
    if (idx >= pages.length) {
      return GetPostListResp(list: [], total: 100, page: page, pageSize: pageSize);
    }
    return pages[idx];
  }

  @override
  Future<GetPostListResp> fetchUserFavorites({
    required num userId,
    required int page,
    required int pageSize,
  }) async {
    return fetchUserPosts(userId: userId, page: page, pageSize: pageSize);
  }
}

/// 构造一个最小化的 PostItem 占位对象，仅用于测试。
/// 真实字段很多，这里只填必需的。
PostItem _post(num id) {
  return PostItem(
    id: id,
    authorId: 1,
    authorName: 'u',
    authorAvatar: '',
    title: 't$id',
    content: 'c',
    images: const [],
    tags: const [],
    viewCount: 0,
    likeCount: 0,
    commentCount: 0,
    favoriteCount: 0,
    createdAt: 0,
    isLiked: false,
    isFavorited: false,
  );
}

void main() {
  group('UserPostsNotifier', () {
    late _FakeUserPostsRepo repo;

    setUp(() {
      repo = _FakeUserPostsRepo();
    });

    test('loadFirstPage 成功时填充 items', () async {
      repo.addPage([_post(1), _post(2)]);
      final n = UserPostsNotifier(
        repo: repo,
        key: const UserPostsKey(userId: 1, type: UserPostsListType.posts),
      );
      await n.loadFirstPage();
      expect(n.state.items.length, 2);
      expect(n.state.page, 1);
      expect(n.state.hasMore, isFalse); // 返回数 < pageSize(20) → 没有更多
      expect(n.state.error, isNull);
    });

    test('loadNextPage 追加 items 而非覆盖', () async {
      repo.addPage(List.generate(20, (i) => _post(i + 1))); // 满一页
      repo.addPage([_post(21), _post(22)]);
      final n = UserPostsNotifier(
        repo: repo,
        key: const UserPostsKey(userId: 1, type: UserPostsListType.posts),
      );
      await n.loadFirstPage();
      expect(n.state.items.length, 20);
      expect(n.state.hasMore, isTrue);

      await n.loadNextPage();
      expect(n.state.items.length, 22);
      expect(n.state.hasMore, isFalse);
      expect(n.state.page, 2);
    });

    test('loadNextPage 在 hasMore=false 时不发请求', () async {
      repo.addPage([_post(1)]);
      final n = UserPostsNotifier(
        repo: repo,
        key: const UserPostsKey(userId: 1, type: UserPostsListType.posts),
      );
      await n.loadFirstPage();
      final callsBefore = repo.calls;
      await n.loadNextPage();
      expect(repo.calls, callsBefore);
    });

    test('refresh 成功时重置 page=1 并覆盖 items', () async {
      repo.addPage([_post(1), _post(2)]);
      final n = UserPostsNotifier(
        repo: repo,
        key: const UserPostsKey(userId: 1, type: UserPostsListType.posts),
      );
      await n.loadFirstPage();
      repo.pages.clear();
      repo.addPage([_post(99)]);
      await n.refresh();
      expect(n.state.items.length, 1);
      expect(n.state.items.first.id, 99);
      expect(n.state.page, 1);
    });

    test('refresh 失败时保留旧数据', () async {
      repo.addPage([_post(1), _post(2)]);
      final n = UserPostsNotifier(
        repo: repo,
        key: const UserPostsKey(userId: 1, type: UserPostsListType.posts),
      );
      await n.loadFirstPage();
      repo.shouldFail = true;
      await n.refresh();
      expect(n.state.items.length, 2); // 旧数据还在
      expect(n.state.isRefreshing, isFalse);
    });

    test('loadFirstPage 失败时设置 error 字段', () async {
      repo.shouldFail = true;
      final n = UserPostsNotifier(
        repo: repo,
        key: const UserPostsKey(userId: 1, type: UserPostsListType.posts),
      );
      await n.loadFirstPage();
      expect(n.state.error, isNotNull);
      expect(n.state.items, isEmpty);
    });
  });

  group('UserPostsKey', () {
    test('同参数相等、hashCode 相同', () {
      const k1 = UserPostsKey(userId: 1, type: UserPostsListType.posts);
      const k2 = UserPostsKey(userId: 1, type: UserPostsListType.posts);
      expect(k1, equals(k2));
      expect(k1.hashCode, k2.hashCode);
    });

    test('不同 type 不相等', () {
      const k1 = UserPostsKey(userId: 1, type: UserPostsListType.posts);
      const k2 = UserPostsKey(userId: 1, type: UserPostsListType.favorites);
      expect(k1, isNot(equals(k2)));
    });
  });
}
```

- [ ] **Step 2：跑测试确认编译失败**

```
cd xiaobaihe_app
flutter test test/features/profile/application/user_posts_notifier_test.dart
```

**Expected**：编译失败，找不到 `user_posts_notifier.dart` 或 `UserPostsNotifier/UserPostsKey/UserPostsRepository/UserPostsListType`。

### Task 4.4：实现 UserPostsNotifier 让测试通过

**Files:**
- Create: `xiaobaihe_app/lib/features/profile/application/user_posts_notifier.dart`

- [ ] **Step 1：写实现**

创建 `xiaobaihe_app/lib/features/profile/application/user_posts_notifier.dart`：

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../../sdk/data/gateway.dart';
import '../data/user_repository.dart';

/// 列表类型：用户自己发的帖子 / 用户收藏的帖子
enum UserPostsListType { posts, favorites }

/// StateNotifierProvider.family 的 Key
class UserPostsKey {
  final num userId;
  final UserPostsListType type;
  const UserPostsKey({required this.userId, required this.type});

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      (other is UserPostsKey && other.userId == userId && other.type == type);

  @override
  int get hashCode => Object.hash(userId, type);
}

/// 抽象 Repository 接口 —— 供测试注入 Fake
abstract class UserPostsRepository {
  Future<GetPostListResp> fetchUserPosts({
    required num userId,
    required int page,
    required int pageSize,
    int sortBy = 1,
  });

  Future<GetPostListResp> fetchUserFavorites({
    required num userId,
    required int page,
    required int pageSize,
  });
}

class UserPostsState {
  final List<PostItem> items;
  final int page;
  final bool hasMore;
  final bool isLoading;
  final bool isRefreshing;
  final Object? error;

  const UserPostsState({
    this.items = const [],
    this.page = 0,
    this.hasMore = true,
    this.isLoading = false,
    this.isRefreshing = false,
    this.error,
  });

  UserPostsState copyWith({
    List<PostItem>? items,
    int? page,
    bool? hasMore,
    bool? isLoading,
    bool? isRefreshing,
    Object? error,
    bool clearError = false,
  }) {
    return UserPostsState(
      items: items ?? this.items,
      page: page ?? this.page,
      hasMore: hasMore ?? this.hasMore,
      isLoading: isLoading ?? this.isLoading,
      isRefreshing: isRefreshing ?? this.isRefreshing,
      error: clearError ? null : (error ?? this.error),
    );
  }
}

class UserPostsNotifier extends StateNotifier<UserPostsState> {
  final UserPostsRepository repo;
  final UserPostsKey key;
  final int pageSize;

  UserPostsNotifier({
    required this.repo,
    required this.key,
    this.pageSize = 20,
  }) : super(const UserPostsState());

  Future<GetPostListResp> _fetch(int page) {
    if (key.type == UserPostsListType.posts) {
      return repo.fetchUserPosts(
        userId: key.userId,
        page: page,
        pageSize: pageSize,
      );
    } else {
      return repo.fetchUserFavorites(
        userId: key.userId,
        page: page,
        pageSize: pageSize,
      );
    }
  }

  Future<void> loadFirstPage() async {
    state = state.copyWith(isLoading: true, clearError: true);
    try {
      final resp = await _fetch(1);
      state = state.copyWith(
        items: resp.list,
        page: 1,
        hasMore: resp.list.length >= pageSize,
        isLoading: false,
      );
    } catch (e) {
      state = state.copyWith(isLoading: false, error: e);
    }
  }

  Future<void> loadNextPage() async {
    if (!state.hasMore || state.isLoading) return;
    state = state.copyWith(isLoading: true);
    final nextPage = state.page + 1;
    try {
      final resp = await _fetch(nextPage);
      state = state.copyWith(
        items: [...state.items, ...resp.list],
        page: nextPage,
        hasMore: resp.list.length >= pageSize,
        isLoading: false,
      );
    } catch (e) {
      state = state.copyWith(isLoading: false, error: e);
    }
  }

  Future<void> refresh() async {
    state = state.copyWith(isRefreshing: true);
    try {
      final resp = await _fetch(1);
      state = state.copyWith(
        items: resp.list,
        page: 1,
        hasMore: resp.list.length >= pageSize,
        isRefreshing: false,
        clearError: true,
      );
    } catch (_) {
      // 刷新失败保留旧数据
      state = state.copyWith(isRefreshing: false);
    }
  }
}

/// Provider.family —— 按 (userId, type) 缓存 notifier 实例
final userPostsProvider = StateNotifierProvider.family<
    UserPostsNotifier, UserPostsState, UserPostsKey>(
  (ref, key) {
    final notifier = UserPostsNotifier(
      repo: ref.read(userPostsRepositoryProvider),
      key: key,
    );
    notifier.loadFirstPage();
    return notifier;
  },
);

/// 真实 Repository 的 provider —— 具体实现在 user_repository.dart
final userPostsRepositoryProvider = Provider<UserPostsRepository>((ref) {
  return UserRepository();
});
```

> **注意**：此文件 import 了 `../data/user_repository.dart` 的 `UserRepository`，并假设 `UserRepository` 实现了 `UserPostsRepository` 接口。下一个 Task 会修改 UserRepository 让它实现该接口。

- [ ] **Step 2：跑测试（仍会失败但换个错误）**

```
cd xiaobaihe_app
flutter test test/features/profile/application/user_posts_notifier_test.dart
```

**Expected**：可能仍编译失败，因为 `UserRepository` 还没实现 `UserPostsRepository` 接口。继续到 Task 4.5。

### Task 4.5：在 UserRepository 实现 fetchUserPosts / fetchUserFavorites

**Files:**
- Modify: `xiaobaihe_app/lib/features/profile/data/user_repository.dart`

- [ ] **Step 1：修改 import 和类声明**

打开 `user_repository.dart`，整体替换为：

```dart
import '../../../core/api/api_adapter.dart';
import '../../../sdk/api/api.dart';
import '../../../sdk/api/gateway.dart' as gw;
import '../../../sdk/data/gateway.dart';
import '../application/user_posts_notifier.dart';

class UserRepository implements UserPostsRepository {
  Future<GetUserResp> getUserProfile(int userId) {
    return apiCall<GetUserResp>(
      (ok, fail, eventually) => gw.getUser(
        userId,
        ok: ok,
        fail: fail,
        eventually: eventually,
      ),
    );
  }

  Future<void> updateUserProfile(UpdateProfileReq req) {
    return apiCall<UpdateProfileResp>(
      (ok, fail, eventually) => gw.updateProfile(
        req,
        ok: ok,
        fail: fail,
        eventually: eventually,
      ),
    );
  }

  Future<void> followUser(int targetUserId) {
    return apiCall<FollowResp>(
      (ok, fail, eventually) => gw.follow(
        FollowReq(targetUserId: targetUserId),
        ok: ok,
        fail: fail,
        eventually: eventually,
      ),
    );
  }

  Future<void> unfollowUser(int targetUserId) {
    return apiCall<UnfollowResp>(
      (ok, fail, eventually) => gw.unfollow(
        UnfollowReq(targetUserId: targetUserId),
        ok: ok,
        fail: fail,
        eventually: eventually,
      ),
    );
  }

  /// 绕过 SDK，用 apiGet 拼 query 直接调用新后端契约。
  /// 后端规格见 docs/backend/2026-04-11-后端待补接口.md 接口 1。
  @override
  Future<GetPostListResp> fetchUserPosts({
    required num userId,
    required int page,
    required int pageSize,
    int sortBy = 1,
  }) {
    return apiCall<GetPostListResp>(
      (ok, fail, eventually) => apiGet(
        '/api/v1/users/${userId.toInt()}/posts'
        '?page=$page&pageSize=$pageSize&sortBy=$sortBy',
        ok: (data) => ok(GetPostListResp.fromJson(data)),
        fail: fail,
        eventually: eventually,
      ),
    );
  }

  /// 后端规格见 docs/backend/2026-04-11-后端待补接口.md 接口 2。
  @override
  Future<GetPostListResp> fetchUserFavorites({
    required num userId,
    required int page,
    required int pageSize,
  }) {
    return apiCall<GetPostListResp>(
      (ok, fail, eventually) => apiGet(
        '/api/v1/users/${userId.toInt()}/favorites'
        '?page=$page&pageSize=$pageSize',
        ok: (data) => ok(GetPostListResp.fromJson(data)),
        fail: fail,
        eventually: eventually,
      ),
    );
  }
}
```

- [ ] **Step 2：跑 notifier 测试**

```
cd xiaobaihe_app
flutter test test/features/profile/application/user_posts_notifier_test.dart
```

**Expected**：`All tests passed!`，8 个测试用例全绿。

- [ ] **Step 3：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/features/profile/
```

**Expected**：0 error、0 新 warning。

- [ ] **Step 4：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/features/profile/application/user_posts_notifier.dart xiaobaihe_app/lib/features/profile/data/user_repository.dart xiaobaihe_app/test/features/profile/application/user_posts_notifier_test.dart
git commit -m "feat(profile): add UserPostsNotifier state machine with tests"
```

### Task 4.6：创建 UserPostList widget

**Files:**
- Create: `xiaobaihe_app/lib/features/profile/presentation/widgets/user_post_list.dart`

- [ ] **Step 1：写 widget**

创建 `xiaobaihe_app/lib/features/profile/presentation/widgets/user_post_list.dart`：

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../../../core/widgets/error_view.dart';
import '../../../feed/presentation/widgets/post_card.dart';
import '../../application/user_posts_notifier.dart';

/// 分页加载的用户帖子列表组件。
/// 复用于：我的帖子 / 我的收藏 / 他人主页的帖子。
class UserPostList extends ConsumerStatefulWidget {
  final num userId;
  final UserPostsListType type;
  const UserPostList({
    super.key,
    required this.userId,
    required this.type,
  });

  @override
  ConsumerState<UserPostList> createState() => _UserPostListState();
}

class _UserPostListState extends ConsumerState<UserPostList> {
  final ScrollController _scrollCtrl = ScrollController();

  UserPostsKey get _key =>
      UserPostsKey(userId: widget.userId, type: widget.type);

  @override
  void initState() {
    super.initState();
    _scrollCtrl.addListener(_onScroll);
  }

  @override
  void dispose() {
    _scrollCtrl.removeListener(_onScroll);
    _scrollCtrl.dispose();
    super.dispose();
  }

  void _onScroll() {
    if (!_scrollCtrl.hasClients) return;
    if (_scrollCtrl.position.pixels >=
        _scrollCtrl.position.maxScrollExtent - 300) {
      ref.read(userPostsProvider(_key).notifier).loadNextPage();
    }
  }

  @override
  Widget build(BuildContext context) {
    final state = ref.watch(userPostsProvider(_key));
    final notifier = ref.read(userPostsProvider(_key).notifier);

    // 首次加载错误（且列表为空）
    if (state.error != null && state.items.isEmpty) {
      return ErrorView(
        message: state.error.toString(),
        onRetry: () => notifier.loadFirstPage(),
      );
    }

    // 首次加载中
    if (state.isLoading && state.items.isEmpty) {
      return const Center(child: CircularProgressIndicator());
    }

    // 空态
    if (state.items.isEmpty) {
      return Center(
        child: Padding(
          padding: const EdgeInsets.all(32),
          child: Text(
            widget.type == UserPostsListType.posts ? '还没有发布任何帖子' : '还没有收藏任何帖子',
            style: TextStyle(color: Theme.of(context).colorScheme.outline),
          ),
        ),
      );
    }

    return RefreshIndicator(
      onRefresh: () => notifier.refresh(),
      child: ListView.builder(
        controller: _scrollCtrl,
        physics: const AlwaysScrollableScrollPhysics(),
        itemCount: state.items.length +
            ((state.isLoading || !state.hasMore) ? 1 : 0),
        itemBuilder: (context, index) {
          if (index >= state.items.length) {
            if (state.isLoading) {
              return const Padding(
                padding: EdgeInsets.all(16),
                child: Center(child: CircularProgressIndicator()),
              );
            }
            return Padding(
              padding: const EdgeInsets.symmetric(vertical: 20),
              child: Center(
                child: Text(
                  '— 没有更多了 —',
                  style: TextStyle(
                    color: Theme.of(context).colorScheme.outline,
                    fontSize: 12,
                  ),
                ),
              ),
            );
          }
          return PostCard(post: state.items[index]);
        },
      ),
    );
  }
}
```

- [ ] **Step 2：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/features/profile/presentation/widgets/user_post_list.dart
```

**Expected**：0 error。如果 `PostCard` 的构造参数名不是 `post`，根据 `xiaobaihe_app/lib/features/feed/presentation/widgets/post_card.dart` 的实际签名调整（很可能是 `PostCard({required PostItem post})` 或类似形式）。

> **若 `PostCard` 接受其它参数名**：打开 `feed/presentation/widgets/post_card.dart` 查看构造参数并相应修改本文件里 `PostCard(post: state.items[index])` 一行。

- [ ] **Step 3：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/features/profile/presentation/widgets/user_post_list.dart
git commit -m "feat(profile): add UserPostList widget with infinite scroll"
```

### Task 4.7：把 UserPostList 接入 profile_page

**Files:**
- Modify: `xiaobaihe_app/lib/features/profile/presentation/profile_page.dart`

- [ ] **Step 1：修改 import 区**

当前 `profile_page.dart` 第 1-10 行：

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import '../../../core/widgets/cached_avatar.dart';
import '../../../core/widgets/error_view.dart';
import '../../../sdk/data/gateway.dart';
import '../../auth/application/auth_notifier.dart';
import '../../feed/application/feed_notifier.dart';
import '../../feed/presentation/widgets/post_card.dart';
import '../data/user_repository.dart';
```

替换为：

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import '../../../core/widgets/cached_avatar.dart';
import '../../../core/widgets/error_view.dart';
import '../../../sdk/data/gateway.dart';
import '../../auth/application/auth_notifier.dart';
import '../application/user_posts_notifier.dart';
import '../data/user_repository.dart';
import 'widgets/user_post_list.dart';
```

**说明**：删除 `feed_notifier` 和 `post_card` 两个未用 import（原 warning 来源），新增 `user_posts_notifier` 和 `user_post_list`。

- [ ] **Step 2：重写 `_ProfileContent` 的 TabBar/body 逻辑**

`profile_page.dart` 的 `_ProfileContent` 类要做两处改动：

**改动 A**：tab 数量根据场景动态决定（自己 = 2 tab；他人公开 = 2 tab；他人私密 = 0 tab）

找到 `_ProfileContentState.initState`（约第 75-80 行）：

```dart
@override
void initState() {
  super.initState();
  if (widget.isOwnProfile) {
    _tabController = TabController(length: 2, vsync: this);
  }
}
```

整体替换为：

```dart
@override
void initState() {
  super.initState();
  // tabController 的实际创建延后到 build 时（因为需要拿到 GetUserResp.favoritesVisible）
}

TabController? _ensureTabController(bool showFavoritesTab) {
  if (!showFavoritesTab) {
    _tabController?.dispose();
    return _tabController = null;
  }
  if (_tabController == null || _tabController!.length != 2) {
    _tabController?.dispose();
    _tabController = TabController(length: 2, vsync: this);
  }
  return _tabController;
}
```

**改动 B**：`build` 方法里根据 `GetUserResp.favoritesVisible` 决定 tab 结构

找到 `build` 方法中的 `body: userAsync.when(...data: (user) => NestedScrollView(...))` 部分（约第 125-205 行）。整个 `data: (user)` 的返回表达式要改。

当前 `data: (user) => NestedScrollView(...` 的尾部（约第 195-204 行）：

```dart
body: widget.isOwnProfile && _tabController != null
    ? TabBarView(
        controller: _tabController,
        children: [
          _PostListPlaceholder(label: '我的帖子'),
          _PostListPlaceholder(label: '我的收藏'),
        ],
      )
    : _PostListPlaceholder(label: '帖子'),
```

整体替换为：

```dart
body: Builder(builder: (context) {
  // 是否显示"收藏"tab
  final showFavoritesTab = widget.isOwnProfile || user.favoritesVisible;
  final tc = _ensureTabController(showFavoritesTab);

  if (showFavoritesTab && tc != null) {
    return TabBarView(
      controller: tc,
      children: [
        UserPostList(
          userId: widget.userId,
          type: UserPostsListType.posts,
        ),
        UserPostList(
          userId: widget.userId,
          type: UserPostsListType.favorites,
        ),
      ],
    );
  }
  // 只显示帖子列表
  return UserPostList(
    userId: widget.userId,
    type: UserPostsListType.posts,
  );
}),
```

并且 `headerSliverBuilder` 里的 TabBar 条件也要相应调整（约第 183-193 行）：

当前：
```dart
if (widget.isOwnProfile && _tabController != null)
  SliverPersistentHeader(
    pinned: true,
    delegate: _TabBarDelegate(
      TabBar(
        controller: _tabController,
        tabs: const [Tab(text: '我的帖子'), Tab(text: '我的收藏')],
      ),
      theme.colorScheme.surface,
    ),
  ),
```

替换为：
```dart
if ((widget.isOwnProfile || user.favoritesVisible) && _tabController != null)
  SliverPersistentHeader(
    pinned: true,
    delegate: _TabBarDelegate(
      TabBar(
        controller: _tabController,
        tabs: [
          Tab(text: widget.isOwnProfile ? '我的帖子' : '帖子'),
          Tab(text: widget.isOwnProfile ? '我的收藏' : '收藏'),
        ],
      ),
      theme.colorScheme.surface,
    ),
  ),
```

- [ ] **Step 3：删除 `_PostListPlaceholder` 类**

找到 `_PostListPlaceholder` 类定义（约第 223-231 行）：

```dart
class _PostListPlaceholder extends StatelessWidget {
  final String label;
  const _PostListPlaceholder({required this.label});

  @override
  Widget build(BuildContext context) {
    return const Center(child: EmptyView(message: '暂无内容'));
  }
}
```

**整段删除**。

- [ ] **Step 4：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/features/profile/presentation/profile_page.dart
```

**Expected**：0 error、0 新 warning。原先的两个 unused import warning 和 unused `_hasMoreComments` 应已消失。

- [ ] **Step 5：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/features/profile/presentation/profile_page.dart
git commit -m "feat(profile): wire UserPostList into profile tabs"
```

### Task 4.8：在 Mock router 增加 /users/:id/posts 和 /favorites 路由

**Files:**
- Modify: `xiaobaihe_app/lib/mock/mock_router.dart`

- [ ] **Step 1：在 `_route` 函数的 switch 中增加 `'users'` 分支**

找到 `_route` 函数的 switch 语句（约第 58-91 行），在 `case 'user':` 分支之后、`case 'media':` 之前插入：

```dart
    case 'users':
      return _handleUsersList(method, segments, query);
```

- [ ] **Step 2：在文件末尾追加 `_handleUsersList` 实现**

在文件末尾（最后一个 `}` 之前）追加：

```dart

// ─── Users (plural) — 用户的帖子/收藏列表 ───

Map<String, dynamic> _handleUsersList(
  String method,
  List<String> segments,
  Map<String, String> query,
) {
  // segments: ['api', 'v1', 'users', ':userId', 'posts'|'favorites']
  if (method != 'GET' || segments.length < 5) return {};

  final userId = int.tryParse(segments[3]) ?? 0;
  final kind = segments[4];
  final page = int.tryParse(query['page'] ?? '1') ?? 1;
  final pageSize = int.tryParse(query['pageSize'] ?? '20') ?? 20;

  List<Map<String, dynamic>> filtered;
  if (kind == 'posts') {
    // 该用户发布的所有帖子
    filtered = _posts.where((p) => (p['authorId'] as num).toInt() == userId).toList()
      ..sort((a, b) => (b['createdAt'] as int).compareTo(a['createdAt'] as int));
  } else if (kind == 'favorites') {
    // Mock 逻辑：仅用户 1 有真实收藏状态，其它用户返回空
    if (userId == 1) {
      filtered = _posts
          .where((p) => _favoritedPostIds.contains((p['id'] as num).toInt()))
          .toList();
    } else {
      filtered = [];
    }
  } else {
    return {};
  }

  final start = (page - 1) * pageSize;
  final end = start + pageSize;
  final slice = start >= filtered.length
      ? <Map<String, dynamic>>[]
      : filtered.sublist(start, end.clamp(0, filtered.length));

  return {
    'list': slice,
    'total': filtered.length,
    'page': page,
    'pageSize': pageSize,
  };
}
```

- [ ] **Step 3：`flutter analyze`**

```
cd xiaobaihe_app
flutter analyze lib/mock/mock_router.dart
```

**Expected**：0 error、0 warning。

- [ ] **Step 4：checkpoint（可选 commit）**

```
git add xiaobaihe_app/lib/mock/mock_router.dart
git commit -m "feat(mock): add /users/:id/posts and /favorites routes"
```

### Task 4.9：手动验证 Phase 4

- [ ] **Step 1：热重启应用**

```
cd xiaobaihe_app
flutter run -t lib/main_mock.dart
```

- [ ] **Step 2：验证"自己主页看到'我的帖子'和'我的收藏'两个 Tab"**

1. 登录（userId=1）
2. 点底部 tab "个人中心"
3. **Expected**：
   - 显示小白鸽的资料
   - TabBar 有两个 tab："我的帖子"和"我的收藏"
   - "我的帖子" tab 默认显示 → 显示用户 1 发布的帖子（post 3 和 post 6，按 createdAt 倒序）
   - 切到"我的收藏" tab → 空态（因为刚登录没点过收藏）"还没有收藏任何帖子"
4. 返回首页，随便点一个帖子，点收藏图标
5. 返回个人中心 → "我的收藏" tab 下拉刷新
6. **Expected**：刚收藏的帖子出现在列表

- [ ] **Step 3：验证"他人主页（公开收藏）看两个 Tab"**

1. 首页点用户 2（萌萌哒小兔）的任意帖子
2. 点详情页内的作者头像（跳转到 `/user/2`）
3. **Expected**：
   - 显示萌萌哒小兔的资料
   - TabBar 显示两个 tab："帖子"和"收藏"
   - 帖子 tab 显示用户 2 发布的帖子（post 1、4、7）

- [ ] **Step 4：验证"他人主页（私密收藏）只看帖子列表、无 TabBar"**

1. 回到首页，点用户 3（科技宅小明）的帖子（如"2026年最值得入手的三款手机"）
2. 点作者头像（跳 `/user/3`）
3. **Expected**：
   - 显示科技宅小明的资料
   - **无 TabBar**，直接显示帖子列表（post 2、5、8）

- [ ] **Step 5：验证下拉刷新**

1. 在"我的帖子"tab 下拉
2. **Expected**：显示刷新指示、短暂后恢复，列表仍在

- [ ] **Step 6：验证触底加载（可选）**

Mock 数据每个用户最多 3 条帖子，pageSize=20 所以不会触发真实的下一页。如需完整验证触底加载，可以在 `mock_data.dart` 的 seedPosts 里临时添加用户 1 的 25 条帖子，hot reload 后验证。本步骤可跳过（单测已覆盖状态机逻辑）。

如以上步骤都符合预期，Phase 4 完成。

---

## Phase 5：验证收尾与文档归档

依赖：Phase 1-4 全部完成。

### Task 5.1：全量静态分析

- [ ] **Step 1：运行完整 analyze**

```
cd xiaobaihe_app
flutter analyze
```

**Expected**：
- 0 error
- 0 warning（原先 3 条 warning 应全部消失）
- 可接受的 info 级告警：`sdk/api/api.dart` 的 5 条 `avoid_print`、`sdk/api/gateway.dart` 的 6 条 `unnecessary_brace_in_string_interps`、`sdk/vars/kv.dart` 的 1 条 `avoid_print`、`api_adapter.dart` 的 2 条 `unintended_html_in_doc_comment` —— 这些都是 SDK 生成文件或沿用现状，不在本次 scope

如果有新的 warning，按提示就地修复；如果有 error，按 error 提示回到对应 Task 排查。

### Task 5.2：跑所有单元测试

- [ ] **Step 1：跑 test/ 目录全部测试**

```
cd xiaobaihe_app
flutter test
```

**Expected**：
- `test/core/auth/jwt_decoder_test.dart` — 10 passed
- `test/features/profile/application/user_posts_notifier_test.dart` — 8 passed
- `test/widget_test.dart` — 原有的脚手架测试（若存在且无关本次改动，pass 或 skip）
- 总计至少 18 个测试全绿

### Task 5.3：手动验证矩阵全量跑一遍

- [ ] **Step 1：按设计文档 7. 测试策略的"手动验证矩阵"逐条验证**

打开 `docs/superpowers/specs/2026-04-11-mvp-收尾-design.md` 的第 7 节，对照"Mock"列跑完每一项。每项 ✓ 的打勾记录到 plan 末尾的实施日志。

矩阵条目（复述）：
- [ ] 冷启动已登录 → 直接进 /profile 不跳登录
- [ ] 冷启动未登录 → 进 /profile 跳登录
- [ ] Token 坏掉（手动改 SharedPreferences）→ 降级未登录
  - 验证方式：应用登录后，关闭应用。在设备上找到 SharedPreferences 存储文件（Windows 桌面版在 `%APPDATA%/<appname>/shared_preferences.json` 或类似位置），把 `tokens` 的 JSON 里的 `access_token` 改成 `"garbage"`，重启应用。**Expected**：以未登录状态启动。
  - 如定位存储文件有困难，在 `auth_notifier.dart::_init` 开头临时 `await removeTokens();` 然后重启，效果相同（测完记得撤销）。
- [ ] 帖子详情滑到评论底部 → 自动加载下一页
- [ ] 切换评论排序 → 分页状态完全重置
- [ ] 自己主页看到"我的帖子" + "我的收藏" 两 tab
- [ ] 他人主页（萌萌哒小兔，favoritesVisible=true）看到帖子 + 收藏两个 Tab
- [ ] 他人主页（科技宅小明，favoritesVisible=false）只看到帖子列表、无 TabBar
- [ ] 发帖选 3 张图 → 全成功 → 发布成功
- [ ] 发帖选 3 张图 → 第 2 张失败 → 整单失败、草稿保留（按 Task 3.5 Step 3 的方法临时篡改 `_handleMedia` 制造失败）

### Task 5.4：追加实施日志到 plan 末尾（L2 模式）

**Files:**
- Modify: `docs/superpowers/plans/2026-04-11-mvp-收尾-plan.md`

- [ ] **Step 1：在本文件末尾追加"实施记录"一节**

在本 plan 文件最末尾追加以下内容，并填写实际执行情况：

```markdown

---

## 实施记录

### 日期：YYYY-MM-DD

**Phase 1（#6 userId 恢复）**
- 完成 Task：1.1、1.2、1.3、1.4、1.5
- 遇到的坑：<如实填写，例如"base64url padding 导致单测第 6 条一开始失败，补上 normalize 后通过">
- Mock 验证：✓ / ✗

**Phase 2（#1 评论分页）**
- 完成 Task：2.1、2.2、2.3、2.4、2.5
- 遇到的坑：
- Mock 验证：

**Phase 3（#4 图片上传 multipart）**
- 完成 Task：3.1、3.2、3.3、3.4、3.5
- 遇到的坑：
- Mock 验证：

**Phase 4（#2 用户帖子/收藏列表）**
- 完成 Task：4.1、4.2、4.3、4.4、4.5、4.6、4.7、4.8、4.9
- 遇到的坑：
- Mock 验证：

**Phase 5（验证收尾）**
- flutter analyze：0 error / 0 warning / N info
- flutter test：N passed / 0 failed
- 手动验证矩阵：N/N 通过

### 真实后端联调阻塞项

以下场景**需要等后端接口完成后再验证**（见 `docs/backend/2026-04-11-后端待补接口.md`）：

- [ ] 真实登录返回的 JWT token 能被 `extractUserIdFromToken` 正确解析（后端 JWT payload 字段名为 `userId`）
- [ ] `GET /api/v1/users/:userId/posts` 真实后端联调
- [ ] `GET /api/v1/users/:userId/favorites` 真实后端联调（含权限 403 兜底）
- [ ] `POST /api/v1/media/image` multipart 真实后端联调
- [ ] `GetUserResp.favoritesVisible` 字段后端实际返回

后端三个新接口就绪后，回到本 plan 跑一遍设计文档 7. 测试策略矩阵的"真实"列。

### 已知缺口（下次 iteration 处理）

承接设计文档第 10 节，本次显式未做：

1. `isFollowing` 真实状态接入 —— 等 `GetUserResp` 增 `isFollowing` 字段后做
2. `edit_profile_page` 增加"收藏可见性"开关 —— 写侧 UI，下次 iteration
3. `/api/v1/me` 接口集成 —— 冷启动拿完整用户信息
4. RefreshToken 机制
5. 测试覆盖（除 P0 两项外）
```

- [ ] **Step 2：填写实际内容**

把占位符 `YYYY-MM-DD` 改成执行日期、把每个 Phase 下的「遇到的坑」填上实际情况、把 Mock 验证改为 ✓。

- [ ] **Step 3：checkpoint（可选 commit）**

```
git add docs/superpowers/plans/2026-04-11-mvp-收尾-plan.md
git commit -m "docs(plan): append implementation log for MVP polishing iteration"
```

---

## 验证完成定义（Definition of Done）

本次迭代达成以下条件即视为完成：

1. ✅ `flutter analyze` 0 error 0 warning（info 级 SDK 遗留可接受）
2. ✅ `flutter test` 全绿，含至少 18 个新增用例（JWT decoder 10 + UserPostsNotifier 8）
3. ✅ Mock 模式手动验证矩阵 10 项全部 ✓
4. ✅ 设计文档 / 后端接口规格 / 本实施计划三份文档齐备
5. ✅ 本 plan 末尾"实施记录"节填写完毕
6. ⏸ 真实后端联调作为独立后续任务（显式登记为阻塞项）

---

## 实施记录

### 日期：2026-04-13

**Phase 1（#6 userId 恢复）**
- 完成 Task：1.1、1.2、1.3、1.4、1.5
- 遇到的坑：base64url 解码需处理 padding；登录/注册流程沿用假 JWT 保持本地 Mock 行为一致。
- Mock 验证：✓

**Phase 2（#1 评论分页）**
- 完成 Task：2.1、2.2、2.3、2.4、2.5
- 遇到的坑：排序切换时需同步清除触底节流状态，避免切换后首页请求被跳过。
- Mock 验证：✓

**Phase 3（#4 图片上传 multipart）**
- 完成 Task：3.1、3.2、3.3、3.4、3.5
- 遇到的坑：Mock HttpClient 需补 `add()` 方法接收 multipart body；事务化失败时用 `Future.wait` 任一 fail 即整体回滚并保留草稿。
- Mock 验证：✓

**Phase 4（#2 用户帖子/收藏列表）**
- 完成 Task：4.1、4.2、4.3、4.4、4.5、4.6、4.7、4.8、4.9
- 遇到的坑：`GetUserResp` 新增字段需同步 Mock seed 数据；他人主页私密收藏时整体隐藏 TabBar 而不是 Tab 内降级。
- Mock 验证：✓

**Phase 5（验证收尾）**
- flutter analyze：0 error / 0 warning / 19 info（全部为 SDK 生成文件及既有代码遗留，不在本次 scope）
- flutter test：19 passed / 0 failed（JWT decoder 10 + UserPostsNotifier 8 + smoke 1）
- 手动验证矩阵：10/10 通过

### 真实后端联调阻塞项

以下场景**需要等后端接口完成后再验证**（见 `docs/backend/2026-04-11-后端待补接口.md`）：

- [ ] 真实登录返回的 JWT token 能被 `extractUserIdFromToken` 正确解析（后端 JWT payload 字段名为 `userId`）
- [ ] `GET /api/v1/users/:userId/posts` 真实后端联调
- [ ] `GET /api/v1/users/:userId/favorites` 真实后端联调（含权限 403 兜底）
- [ ] `POST /api/v1/media/image` multipart 真实后端联调
- [ ] `GetUserResp.favoritesVisible` 字段后端实际返回

后端三个新接口就绪后，回到本 plan 跑一遍设计文档 7. 测试策略矩阵的"真实"列。

### 已知缺口（下次 iteration 处理）

承接设计文档第 10 节，本次显式未做：

1. `isFollowing` 真实状态接入 —— 等 `GetUserResp` 增 `isFollowing` 字段后做
2. `edit_profile_page` 增加"收藏可见性"开关 —— 写侧 UI，下次 iteration
3. `/api/v1/me` 接口集成 —— 冷启动拿完整用户信息
4. RefreshToken 机制
5. 测试覆盖（除 P0 两项外）
